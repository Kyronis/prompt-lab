import type { Config } from 'tailwindcss';

// Tailwind v4 design tokens migrated to globals.css @theme inline block
const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
};

export default config;
